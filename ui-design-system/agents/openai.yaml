interface:
  display_name: "UI Design System"
  short_description: "Create, update, or audit a project UI Design System and AI guardrails"
  default_prompt: "Use $ui-design-system to inspect the current project's UI rules and shared components, then create or incrementally update docs/project/design/DESIGN_SYSTEM.md. Install project-agent enforcement only when I explicitly ask. Do not implement product UI in this step."
