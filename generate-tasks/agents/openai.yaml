interface:
  display_name: "generate-tasks"
  short_description: "Generate task definitions, verification plans, and prompts in real execution order"
  default_prompt: "Use $generate-tasks to update docs/project/tasks.md from the current PRD and repository facts, then regenerate docs/project/task-prompt.md in the recommended execution order."
